<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.shiki.domain.dao.SPermsExtMapper">
    <resultMap id="BaseResultMap" type="com.shiki.domain.dto.SPerms">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Jun 24 14:11:35 CST 2019.
        -->
        <id column="perms_id" property="permsId" jdbcType="INTEGER"/>
        <result column="perms_name" property="permsName" jdbcType="VARCHAR"/>
    </resultMap>
    <select id="findAllByRoleId" resultMap="BaseResultMap">
        SELECT p.perms_id,
               p.perms_name
        FROM s_perms as p
                 natural join s_role_perms as r_p
                 natural join s_role as r
        WHERE r.role_id = #{roleId,jdbcType=INTEGER}
    </select>
    <select id="findPermsByUserId" resultType="string" parameterType="java.lang.Integer">
        select distinct p.perms_name
        from s_user u
                 natural join s_user_role u_r
                 natural join s_role r
                 natural join s_role_perms r_p
                 natural join s_perms p
        where u.user_id = #{userId,jdbcType=INTEGER}
    </select>
</mapper>